"""Benchmarks for the Peblar client, measured via CodSpeed.

These tests are skipped by default (see `[tool.pytest.ini_options] addopts`)
and only run in the benchmarks CI workflow with `pytest --codspeed`.
"""
